using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace KJ
{
    [CreateAssetMenu(fileName = "Scene Enum List Creater", menuName = "ScriptableObjects/Scene/Scene Enum List Creater")]
    public class CreateSceneEnumList : ScriptableObject
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}